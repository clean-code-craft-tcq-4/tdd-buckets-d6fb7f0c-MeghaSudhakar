name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: tshirts pass
      run: |
        g++ -Wall -Wextra -Werror TDD.c, test.cpp -o TDD.out
        ./TDD.out
